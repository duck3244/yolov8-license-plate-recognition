# YOLOv8 번호판 인식 시스템 의존성 (v3.0.0 - FastAPI 백엔드)
#
# 권장 환경: Python 3.10 + PyTorch 2.x + CUDA 12.1 (RTX 4060 / Ada Lovelace)
# Pororo OCR은 이 저장소에 vendored 되어 있어 별도 설치가 필요하지 않습니다.
# (소스: ./pororo/, 호환 레이어: ./pororo/_compat.py)

# ===== 핵심 라이브러리 =====
ultralytics>=8.0.0,<9.0.0
opencv-python>=4.5.0,<5.0.0
pytesseract>=0.3.8
numpy>=1.24.0
Pillow>=9.0.0

# ===== 딥러닝 프레임워크 =====
# CUDA 빌드는 PyTorch 공식 인덱스에서 설치 권장:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

# ===== 모델 다운로드 =====
huggingface-hub>=0.19.0

# ===== 웹 프레임워크 (FastAPI) =====
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.4.0

# ===== 데이터 처리 / 시각화 =====
pandas>=1.3.0
matplotlib>=3.3.0,<4.0.0
seaborn>=0.11.0

# ===== 설정 / 환경 =====
pyyaml>=6.0
python-dotenv>=0.19.0

# ===== 모니터링 =====
prometheus-client>=0.12.0
psutil>=5.8.0
GPUtil>=1.4.0

# ===== 네트워킹 / 백그라운드 =====
requests>=2.25.0
schedule>=1.1.0

# ===== 선택적 OCR 엔진 (필요 시 주석 해제) =====
# easyocr>=1.6.2
# paddleocr>=2.6.0
# paddlepaddle-gpu>=2.4.0
